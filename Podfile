# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'SDWebRTC' do
  # Uncomment the next line if you're using Swift or would like to use dynamic frameworks
  # use_frameworks!

  # Pods for SDWebRTC
use_frameworks!
platform :ios, '9.0'
pod 'GoogleWebRTC'
pod 'SIOSocket'

  target 'SDWebRTCTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'SDWebRTCUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
